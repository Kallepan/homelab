# Update Angular

## Update Angular CLI

```bash
sudo npm uninstall -g @angular/cli
sudo npm install -g @angular/cli@latest
```

## Update Dependencies

- Simple commands to update Angular to the latest version and update all dependencies.

```bash
sudo npm i -g npm-check-updates
sudo ncu -u
sudo npm install
```

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dashboard-pvc
  namespace: kuma-system
spec:
  storageClassName: longhorn
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
---
apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: kuma-system
  labels:
    app: dashboard
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
  selector:
    app: dashboard
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: dashboard
  namespace: uptime-kuma
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`uptime.srv-k8s.server.io`)
      priority: 10
      services:
        - name: dashboard
          port: 3001
  tls:
    secretName: dashboard-tls-secret
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: dashboard-redirect
  namespace: uptime-kuma
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`uptime.srv-k8s.server.io`)
      priority: 10
      middlewares:
        - name: redirect-to-https
      services:
        - name: dashboard
          port: 3001
---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: redirect-to-https
  namespace: uptime-kuma
spec:
  redirectScheme:
    scheme: https
    permanent: true
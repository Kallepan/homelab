---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: minio-operator
  namespace: minio
spec:
  interval: 1h
  chart:
    spec:
      chart: operator
      version: "5.x.x"
      sourceRef:
        kind: HelmRepository
        name: minio-operator
        namespace: minio
  values:
    console:
      ingress:
        number: 9090
        enabled: true
        ingressClassName: traefik
        host: minio.prod.server.home
        tls:
          - secretName: minio-tls-secret
            hosts:
              - minio.prod.server.home
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: minio-tenant
  namespace: minio
spec:
  interval: 1h
  chart:
    spec:
      chart: tenant
      version: "5.x.x"
      sourceRef:
        kind: HelmRepository
        name: minio-operator
        namespace: minio
  values:
    tenant:
      metrics:
        enabled: false
        port: 9000
        protocol: http
      certificate:
        requestAutoCert: false
      pools:
        - name: pool-0
          servers: 1 # standalone
          size: 10Gi
          storageClassName: longhorn
    ingress:
      api:
        enabled: true
        ingressClassName: traefik
        host: api.minio.prod.server.home
        tls:
          - secretName: minio-tls-secret
            hosts:
              - api.minio.prod.server.home
      console:
        enabled: true
        ingressClassName: traefik
        host: tenant.minio.prod.server.home
        tls:
          - secretName: minio-tls-secret
            hosts:
              - tenant.minio.prod.server.home

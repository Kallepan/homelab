defaultNamespace: metallb-system

labels:
  app: metallb
  tier: bootstrap
# Normally, I use Helm to install MetalLB, but Fleet errors out due to a modified apiextensions.k8s.io/v1 CRD.
# As a workaround, I installed MetalLB using Rancher's apps API.

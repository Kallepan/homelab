apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: podinfo
  namespace: default
spec:
  interval: 5m
  chart:
    spec:
      chart: podinfo
      version: 6.5.4
      interval: 5m
      sourceRef:
        kind: HelmRepository
        name: podinfo
        namespace: default
  values:
    replicaCount: 1
# Private Key Infrastructure (PKI)

This directory contains the scripts and configuration files to create a private key infrastructure (PKI) for the project. The PKI is used to create the certificates and keys for the various services in the project.

The PKI is created using simple bash scripts and the `openssl` command line tool.
